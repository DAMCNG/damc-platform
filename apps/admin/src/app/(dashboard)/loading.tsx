import { LoadingLogo } from "@damc/ui";

export default function Loading() {
  return (
    <div className="flex min-h-[70vh] items-center justify-center">
      <LoadingLogo label="Loading" />
    </div>
  );
}
